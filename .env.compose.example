# Copy to .env.compose for local docker-compose:
#   cp .env.compose.example .env.compose
#
# Not used by backend dev (.env) — only for: docker compose --env-file .env.compose up
JWT_SECRET=change-me-local-dev-only
